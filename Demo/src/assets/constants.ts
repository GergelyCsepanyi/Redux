enum DropdownDataValue {
  latest = 'latest',
  oldest = 'oldest',
  popular = 'popular',
}

export default {
  INITIAL_IMAGE_PAGENUMBER: 1,
  DEFAULT_PHOTO_ORDER: DropdownDataValue.latest,
  DropdownDataValue,
};
